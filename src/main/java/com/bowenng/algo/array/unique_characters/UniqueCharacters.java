package com.bowenng.algo.array.unique_characters;

/*
 * Determines if a char[] contains only unique characters
 * Edge cases:
 *  1. return true when char[] == null
 *  2. return true when char[].length == 0
 */
public interface UniqueCharacters {
    boolean areCharactersUnique(char[] chars);
}
